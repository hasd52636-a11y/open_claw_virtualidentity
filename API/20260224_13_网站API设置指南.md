API密钥",
  "code": "UNAUTHORIZED"
}
```

## 🔧 测试方法

### curl测试命令
```bash
# 测试身份生成
curl -X POST http://localhost:3000/api/v1/identities/generate \
  -H "Content-Type: application/json" \
  -H "X-API-Key: sk_your_secret_key_here" \
  -d '{"country": "US", "includeFinancial": true}'

# 测试批量生成
curl -X POST http://localhost:3000/api/v1/identities/bulk-generate \
  -H "Content-Type: application/json" \
  -H "X-API-Key: sk_your_secret_key_here" \
  -d '{"count": 5, "country": "CN"}'

# 测试健康检查
curl http://localhost:3000/health
```

### 自动化测试
```javascript
// test/api.test.js
const request = require('supertest');
const app = require('../app');

describe('Identity API', () => {
  it('should generate identity with valid API key', async () => {
    const response = await request(app)
      .post('/api/v1/identities/generate')
      .set('X-API-Key', process.env.API_KEY)
      .send({ country: 'US' });
    
    expect(response.status).toBe(200);
    expect(response.body).toHaveProperty('data.personal.firstName');
    expect(response.body).toHaveProperty('hash');
    expect(response.body).toHaveProperty('signature');
  });
});
```

## 🎯 总结

### 网站需要实现的核心功能
1. **API端点** - `/api/v1/identities/generate` 和批量生成端点
2. **身份生成** - 支持多国家的虚拟身份生成
3. **哈希计算** - 基于内容生成唯一哈希值
4. **签名生成** - 使用哈希派生的密钥生成数字签名
5. **安全验证** - API密钥验证和速率限制

### 部署要求
1. **Node.js环境** - 版本16+
2. **环境变量** - API密钥和哈希盐值
3. **网络访问** - 允许OpenClaw服务器访问
4. **HTTPS支持** - 生产环境建议使用HTTPS

**大王，您的网站按照这个指南设置，就可以完美与OpenClaw对接了！** 所有代码都是完整的，可以直接使用。